name: project_name
description: project_description
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=2.18.5 <3.0.0'

dependencies:
  flutter:
    sdk: flutter
  # Múltiplos resultados para tratar casos de sucesso e erro
  result_dart: ^1.0.0+2
  # Geração de guids
  uuid: ^3.0.7

dev_dependencies:
  flutter_lints: ^2.0.0
  flutter_test:
    sdk: flutter
  test:


flutter:
  uses-material-design: true